{
  "name": "opencode-agent-messaging",
  "version": "0.1.0",
  "description": "Keep parallel opencode sessions in the same worktree up to date with each other's code changes.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "bun build src/index.ts --target=node --outfile=dist/index.js --external @opencode-ai/plugin && tsc --emitDeclarationOnly",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "prepublishOnly": "bun run typecheck && bun test && bun run build"
  },
  "keywords": [
    "opencode",
    "opencode-plugin",
    "plugin",
    "agent",
    "messaging",
    "multi-agent",
    "coordination"
  ],
  "license": "AGPL-3.0-only",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/TheArctesian/open-code-agent-messaging.git"
  },
  "bugs": {
    "url": "https://github.com/TheArctesian/open-code-agent-messaging/issues"
  },
  "homepage": "https://github.com/TheArctesian/open-code-agent-messaging#readme",
  "peerDependencies": {
    "@opencode-ai/plugin": "*"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "1.18.18",
    "@types/node": "^24.0.0",
    "typescript": "^5.8.2"
  }
}
